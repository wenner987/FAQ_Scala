<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="grp.faq.dao.UserDAO">
    <resultMap id="UserResult" type="grp.faq.entity.UserEntity">
        <result property="uid" column="C_UID" javaType="Long" jdbcType="bigint"/>
    </resultMap>
<!--    UserEntity selectByName(String userName);-->
    <select id="selectByName" resultType="grp.faq.entity.UserEntity" parameterType="java.lang.String">
        SELECT C_ FROM TB_USER WHERE #(userName)
    </select>
<!--    int insertUser(UserEntity user);-->
    <insert id="insertUser" keyProperty="uid" useGeneratedKeys="true">

    </insert>
<!--    int delete(@Param("id")Integer id);-->

<!--    int update(UserEntity user);-->

<!--    List selectAll();-->

<!--    List selectPagination(@Param("index")Integer index);-->
</mapper>